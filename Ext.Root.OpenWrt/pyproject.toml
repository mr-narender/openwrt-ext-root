[project]
name = "ext-root-openwrt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paramiko>=3.4.1",
    "yaspin>=3.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
